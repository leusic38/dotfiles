# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
set $moda Mod1


set $shutdown sudo -A shutdown -h now
set $reboot sudo -A reboot -h now
# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.

################
#  Variables   #
################
set $monitorLeft   DVI-I-0
set $monitorRight  DVI-I-3

#################
#   Apparence   #
#################
font pango:SFNS Display 10

# Couleurs
set_from_resource       $active       active
set_from_resource       $inactive     secondary
set_from_resource       $background   background
set_from_resource       $foreground   foreground

# class			border	    backgr.   	text	       indicat   child_border
client.focused          $background #303040     $foreground    #2e9ef4   $active
client.focused_inactive $background #303040 	$foreground    #484e50   $inactive
client.unfocused        $background $background $inactive      #798284   $inactive
client.urgent           $active     $active    	$background    #900000   $active
client.placeholder      $background $background $foreground    #000000   $inactive
client.background       $background

# Fond d'écran
exec_always --no-startup-id feh --bg-scale ~/Images/Wallpapers/wallpaper2.jpg

# Bar superieur
#bar {
#  status_command i3blocks
#  font pango:Inconsolata, Icons 12
#  position top
#}

# focus sur la souris
focus_follows_mouse yes 

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4D.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec urxvt -cd "`xcwd`"

# kill focused window
bindsym $mod+Shift+a kill

# start dmenu (a program launcher)
bindsym $mod+d exec --no-startup-id dmenu_run

# lock screen
#exec --no-startup-id betterlockscreen -u ~/Images/Wallpapers
#bindsym $mod+shift+x exec betterlockscreen --lock blur

# There also is the (new) i3-dmenu-desktop which only displays applications

# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+j focus left
bindsym $mod+m focus down
bindsym $mod+i focus up
bindsym $mod+k focus right

# $modaernatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+m move down
bindsym $mod+Shift+i move up
bindsym $mod+Shift+k move right

# $modaernatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+t layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+q focus parent

# focus the child container
#bindsym $mod+d focus child

bindsym $mod+Ctrl+i exec intellij-idea-ultimate-edition

#################
##  WORKSPACE  ##
#################

# Workspaces
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"
set $ws11 "11"
set $ws12 "12"
set $ws13 "13"
set $ws14 "14"
set $ws15 "15"
set $ws16 "16"
set $ws17 "17"
set $ws18 "18"
set $ws19 "19"
set $ws20 "20"

#workspace monitor association

workspace $ws2 output $monitorRight
workspace $ws4 output $monitorRight
workspace $ws6 output $monitorLeft
workspace $ws8 output $monitorLeft
workspace $ws10 output $monitorLeft
workspace $ws12 output $monitorRight
workspace $ws14 output $monitorRight
workspace $ws16 output $monitorLeft
workspace $ws18 output $monitorLeft
workspace $ws20 output $monitorLeft

workspace $ws1 output $monitorRight
workspace $ws3 output $monitorRight
workspace $ws5 output $monitorRight
workspace $ws7 output $monitorLeft
workspace $ws9 output $monitorLeft
workspace $ws11 output $monitorRight
workspace $ws13 output $monitorRight
workspace $ws15 output $monitorRight
workspace $ws17 output $monitorLeft
workspace $ws19 output $monitorLeft


# switch to workspace
#bindsym $mod+ampersand workspace $ws1
bindsym $mod+eacute workspace $ws2
bindsym $mod+quotedbl workspace $ws3
bindsym $mod+apostrophe workspace $ws4
#bindsym $mod+parenleft workspace $ws5
bindsym $mod+minus workspace $ws6
bindsym $mod+egrave workspace $ws7
bindsym $mod+underscore workspace $ws8
bindsym $mod+ccedilla workspace $ws9
bindsym $mod+agrave workspace $ws10
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10
#bindsym $mod+Ctrl+ampersand workspace $ws11
bindsym $mod+Ctrl+eacute workspace $ws12
bindsym $mod+Ctrl+quotedbl workspace $ws13
bindsym $mod+Ctrl+apostrophe workspace $ws14
#bindsym $mod+Ctrl+parenleft workspace $ws15
bindsym $mod+Ctrl+minus workspace $ws16
bindsym $mod+Ctrl+egrave workspace $ws17
bindsym $mod+Ctrl+underscore workspace $ws18
bindsym $mod+Ctrl+ccedilla workspace $ws19
bindsym $mod+Ctrl+agrave workspace $ws20
bindsym $mod+Ctrl+1 workspace $ws11
bindsym $mod+Ctrl+2 workspace $ws12
bindsym $mod+Ctrl+3 workspace $ws13
bindsym $mod+Ctrl+4 workspace $ws14
bindsym $mod+Ctrl+5 workspace $ws15
bindsym $mod+Ctrl+6 workspace $ws16
bindsym $mod+Ctrl+7 workspace $ws17
bindsym $mod+Ctrl+8 workspace $ws18
bindsym $mod+Ctrl+9 workspace $ws19
bindsym $mod+Ctrl+0 workspace $ws20

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10
bindsym $mod+Shift+ampersand move container to workspace $ws1
bindsym $mod+Shift+eacute move container to workspace $ws2
bindsym $mod+Shift+quotedbl move container to workspace $ws3
bindsym $mod+Shift+apostrophe move container to workspace $ws4
bindsym $mod+Shift+parenleft move container to workspace $ws5
bindsym $mod+Shift+minus move container to workspace $ws6
bindsym $mod+Shift+egrave move container to workspace $ws7
bindsym $mod+Shift+underscore move container to workspace $ws8
bindsym $mod+Shift+ccedilla move container to workspace $ws9
bindsym $mod+Shift+agrave move container to workspace $ws10
bindsym $mod+Ctrl+Shift+1 move container to workspace $ws11
bindsym $mod+Ctrl+Shift+2 move container to workspace $ws12
bindsym $mod+Ctrl+Shift+3 move container to workspace $ws13
bindsym $mod+Ctrl+Shift+4 move container to workspace $ws14
bindsym $mod+Ctrl+Shift+5 move container to workspace $ws15
bindsym $mod+Ctrl+Shift+6 move container to workspace $ws16
bindsym $mod+Ctrl+Shift+7 move container to workspace $ws17
bindsym $mod+Ctrl+Shift+8 move container to workspace $ws18
bindsym $mod+Ctrl+Shift+9 move container to workspace $ws19
bindsym $mod+Ctrl+Shift+0 move container to workspace $ws20
bindsym $mod+Ctrl+Shift+ampersand move container to workspace $ws11
bindsym $mod+Ctrl+Shift+eacute move container to workspace $ws12
bindsym $mod+Ctrl+Shift+quotedbl move container to workspace $ws13
bindsym $mod+Ctrl+Shift+apostrophe move container to workspace $ws14
bindsym $mod+Ctrl+Shift+parenleft move container to workspace $ws15
bindsym $mod+Ctrl+Shift+minus move container to workspace $ws16
bindsym $mod+Ctrl+Shift+egrave move container to workspace $ws17
bindsym $mod+Ctrl+Shift+underscore move container to workspace $ws18
bindsym $mod+Ctrl+Shift+ccedilla move container to workspace $ws19
bindsym $mod+Ctrl+Shift+agrave move container to workspace $ws20

# force appli in workspace
# Workspace 1(Chrome)
assign [class="Google-chrome"]                      $ws1
# Workspace 3 (JetBrains)
assign [class="jetbrains-idea"]                            $ws3
# Workspace 4(EDI)
assign [class="Code|SublimeText"]                      $ws11
# Workspace 7(Firefox)
assign [class="Firefox"]                          $ws7
# Workspace 9(Transmission Virtualbox)
assign [class="Transmission|virtualBox"]                      $ws9
# Workspace 5(explorateur)
assign [class="Caja|Nemo|ranger"]                          $ws5
# Workspace 6(Spotify)
assign [class="Spotify"]                        $ws12
for_window [class="Spotify"]          move to workspace $ws12
# Workspace 8(mdp)
assign [class="KeePass"]                        $ws16
# Workspace 10(Video)
assign [class="vlc|kodi"]                        $ws11

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# exit i3 (logs you out of your X session)
#bindsym $mod+Shift+e exec --no-startup-id "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"


#Press $mod (super) and X to exit - check toolbar for next choices
bindsym $mod+X exec --no-startup-id ~/.config/i3/scripts/i3exit.sh lock
bindsym control+mod1+Delete mode "$mode_system"
bindsym $mod+Shift+e mode "$mode_system"

set $mode_system System (k) lock, (l) logout, (u) suspend, (h) hibernate, (r) reboot, (s) shutdown
mode "$mode_system" {
    bindsym k exec --no-startup-id ~/.config/i3/scripts/i3exit.sh lock, mode "default"
    bindsym l exec --no-startup-id ~/.config/i3/scripts/i3exit.sh logout, mode "default"
    bindsym u exec --no-startup-id ~/.config/i3/scripts/i3exit.sh suspend, mode "default"
    bindsym h exec --no-startup-id ~/.config/i3/scripts/i3exit.sh hibernate, mode "default"
    bindsym r exec --no-startup-id ~/.config/i3/scripts/i3exit.sh reboot, mode "default"
    bindsym s exec --no-startup-id ~/.config/i3/scripts/i3exit.sh shutdown, mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}


#bindsym $mod4+
#block access
#bindsym $mod+Delete exec rofi-computer
# custom keybinding to lockscreen, use --lock dim or --lock blur below for different backgrounds
#exec --no-startup-id betterlockscreen -u ~/Images/Wallpapers
#bindsym $mod+shift+x exec betterlockscreen --lock blur


#mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
bindsym $mod+$moda+j resize shrink width 5 px or 5 ppt
bindsym $mod+$moda+i resize grow height 5 px or 5 ppt
bindsym $mod+$moda+m resize shrink height 5 px or 5 ppt
bindsym $mod+$moda+k resize grow width 5 px or 5 ppt
bindsym $mod+$moda+shift+j resize shrink width 15 px or 15 ppt
bindsym $mod+$moda+shift+i resize grow height 15 px or 15 ppt
bindsym $mod+$moda+shift+m resize shrink height 15 px or 15 ppt
bindsym $mod+$moda+shift+k resize grow width 15 px or 15 ppt
# same bindings, but for the arrow keys
bindsym $mod+$moda+Left resize shrink width 5 px or 5 ppt
bindsym $mod+$moda+Down resize grow height 5 px or 5 ppt
bindsym $mod+$moda+Up resize shrink height 5 px or 5 ppt
bindsym $mod+$moda+Right resize grow width 5 px or 5 ppt
bindsym $mod+$moda+shift+Left resize shrink width 15 px or 15 ppt
bindsym $mod+$moda+shift+Down resize grow height 15 px or 15 ppt
bindsym $mod+$moda+shift+Up resize shrink height 15 px or 15 ppt
bindsym $mod+$moda+shift+Right resize grow width 15 px or 15 ppt
        # back to normal: Enter or Escape
       # bindsym $mod+$moda+Return mode "default"
       # bindsym $mod+$moda+Escape mode "default"
#}

# resize window (you can also use the mouse for that)
#bindsym $mod+r mode "resize"

# Applications au démarrage
exec_always --no-startup-id conky -c ~/.config/conky/conkyLeft.conf
exec_always --no-startup-id conky -c ~/.config/conky/conkyRight.conf
exec --no-startup-id compton
exec --no-startup-id google-chrome-stable
exec --no-startup-id spotify
#exec --no-startup-id firefox

# Pamac Manager a besoin de ça pour gérer l'authentification
exec --no-startup-id /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
# I3 Gaps
for_window [class="^.*"] border pixel 3
for_window [class=".*"] title_format "  %title"
gaps inner 2
gaps outer 2
smart_borders on
#smart_gaps on

exec_always --no-startup-id setxkbmap -layout frqw,fr -variant ,oss -option -option grp:rctrl_rshift_toggle
for_window [class="Qalculate-gtk" title="qalculate"] floating enable
# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
for_window [class="Yad" title="yad-calendar"] floating enable
exec --no-startup-id launchlistener
exec_always --no-startup-id "killall polybar; polybar -r i3left & polybar -r i3right"
