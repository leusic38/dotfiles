#!/bin/sh

# Syncs repositories and downloads updates, meant to be run as a cronjob.

#ping -q -c exppotentiels.fr > /dev/null || exit
#mirrors
sudo pacman -Syyuwq --noconfirm
notify-send "update list ok"
#yay -Suaw --noconfirm --sudoloop

