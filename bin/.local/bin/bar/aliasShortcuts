#!/bin/sh

##my shortcuts alias

case $BLOCK_BUTTON in
				1) notify-send "My shortcuts
								----------------
								ls : exa --icons
								ll : exa -lg --icons
								la : exa -lag --icons
								ltl (var) : tree
								";;
esac

echo "🩳"
